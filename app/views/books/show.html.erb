<div class="container px-5 px-sm-0">
<%= render 'users/list', user: @book.user %>
<%= render 'books/list', book: @book2 %>
   <div class="col-md-8 offset-md-1">
    <h2>Book details</h2>
    <table class="table table-hover table-inverse">
      <thead>

  				</thead>
  			  <tbody>
  				  <tr>
  					  <td><%=link_to user_path(@book.user) do  %>
  					         <%= image_tag @book.user.get_profile_image(80,80) %><br>
  					         <%= @book.user.name %>
  					        <% end %></td>
  					  <td><%=link_to book_path(@book) do  %>
  					       <%= @book.title %>
  					      <% end %> </td>
  					  <td><%= @book.body %></td>
  					  <td><% if  @book.user == current_user %>
  					      <bottan class="btn btn-sm btn-success"><%= link_to "Edit", edit_book_path(@book), class: 'text-light' %></bottan>
  					      </tb>
  					  <td>
  					  <bottan class="btn btn-sm btn-danger"><%= link_to "Destroy",book_path(@book), method: :delete, class: 'text-light',"data-confirm" => "本当に消しますか?" %></bottan>
  					      <% end %></td>
  				  </tr>
  			  </tbody>
  			</table>
    </div>
  </div>
</div>